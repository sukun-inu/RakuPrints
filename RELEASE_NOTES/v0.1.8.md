# らーく印刷 v0.1.8 リリースノート

## 変更点
- PDF印刷で、用紙の安全領域を基準に「はみ出す場合のみ自動縮小」して印刷するように調整しました。
- プリンターが `pageRect` を返さない場合でも、`paperRect` を使ってセンタリングできるように補正しました。

## 既知の注意
- フチなし印刷に非対応のプリンターでは、物理的な印字不可領域により端が欠ける場合があります。
